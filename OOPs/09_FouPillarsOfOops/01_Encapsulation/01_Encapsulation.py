# Encapsulation means for data security
# it is the practice of bundling attributes(variables) and methods(functions) of a class
# it restricts direct access to some of an object component which protect data integrity
# think encapusation like an capsule pill, it's outer covering protect inside material

# note - class is based on model on encapsulation
#        as by definiting, class is a blueprint that binds attribute(variable) and method(functions)


